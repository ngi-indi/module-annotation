'use strict';

/**
 * frasi-classificate router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::frasi-classificate.frasi-classificate');
