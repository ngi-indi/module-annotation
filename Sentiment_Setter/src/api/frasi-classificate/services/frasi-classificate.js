'use strict';

/**
 * frasi-classificate service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::frasi-classificate.frasi-classificate');
